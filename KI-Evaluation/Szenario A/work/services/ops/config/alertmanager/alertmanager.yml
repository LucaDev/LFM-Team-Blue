route:
  receiver: local-alert-feed
  group_by:
    - alertname
    - job
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-alert-feed
    webhook_configs:
      - url: http://alert-forwarder:8088/alertmanager
        send_resolved: true
