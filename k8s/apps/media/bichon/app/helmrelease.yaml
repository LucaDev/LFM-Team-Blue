# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bichon
spec:
  chartRef:
    kind: OCIRepository
    name: app-template-4
    namespace: flux-system
  interval: 30m
  values:
    controllers:
      bichon:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            fsGroup: 2000
            fsGroupChangePolicy: OnRootMismatch
            runAsGroup: 2000
            runAsNonRoot: true
            runAsUser: 2000
        containers:
          app:
            image:
              repository: docker.io/rustmailer/bichon
              tag: 0.3.7
            env:
              BICHON_HTTP_PORT: &httpPort 8080
              BICHON_LOG_LEVEL: info
              BICHON_JSON_LOGS: "true"
              BICHON_PUBLIC_URL: https://mailbackup.uni.lucadev.de
              BICHON_ROOT_DIR: /data
            envFrom:
              - secretRef:
                  name: bichon
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *httpPort
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    service:
      app:
        ports:
          http:
            port: *httpPort
    persistence:
      data:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        storageClass: hdd-dualreplica
        size: 15Gi
        globalMounts:
          - path: /data
    route:
      app:
        hostnames:
          - mailbackup.uni.lucadev.de
        parentRefs:
          - name: envoy
            namespace: networking
            sectionName: https
